import {
  StyledLoader,
  StyledLoaderHolder,
} from '@/components/common/loader/StyledLoader';

const Loader = () => {
  return (
    <StyledLoaderHolder>
      <StyledLoader />
    </StyledLoaderHolder>
  );
};

export default Loader;
